# rust_project
These are my Rust projects

My first project is a Concurrent Port Scanner

It takes a simple input, which is an ip adress, and a range (X-Y)
It will scan every ports of the input adress from X to Y, including those two

My second project is a ELF header analysis